# Privacy

The Phase 0 feasibility probe is local-only and does not send telemetry or
contact package feeds. Its synthetic fixtures contain no customer data,
credentials, or private package URLs.

The future FeedFence analysis contract is intentionally offline. Diagnostics
must use source keys and bounded configuration labels rather than credentials,
authenticated URLs, full local paths, package contents, or user identifiers.
