# KeelMatrix FeedFence

FeedFence checks whether a restored NuGet graph can come only from the sources
you intended.

The full command-line product is not included in this foundation milestone.
The repository's Phase 0 feasibility probe validates the official NuGet
configuration APIs before durable analysis behavior is added.
